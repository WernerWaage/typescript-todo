{
    // Tasks on build (ctrl+shift+b)
    
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Sass Compile",
            "type": "shell",
            "command": "sass src/styles.scss ./public/styles.css",
            "group": "build"
        }
    ]
}